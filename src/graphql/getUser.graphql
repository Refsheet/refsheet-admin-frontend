query getUser($username: String) {
    getUser(username: $username) {
        id
        created_at
        updated_at
        avatar_url
        name
        username
        email
        email_confirmed_at
        unconfirmed_email
    }
}
