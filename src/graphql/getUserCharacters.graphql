query getUser($id: ID) {
    getUser(id: $id) {
        id
        characters {
            id
            name
            slug
            shortcode
            avatar_url(style: "thumbnail")
            profile_image {
                url {
                    small_square
                }
            }
        }
    }
}
