query getUser($id: ID) {
    getUser(id: $id) {
        id
        characters {
            id
            name
            slug
            shortcode
            profile_image {
                url {
                    small_square
                }
            }
        }
    }
}
